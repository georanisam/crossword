# 가로세로 퀴즈 만들기 — Claude Code 인수인계 문서

이 파일은 **이 프로젝트의 지속 기억**입니다. 새 세션·다른 PC·다른 계정에서도 git과 함께 따라옵니다.
**작업 전에 전체를 읽고**, 의미 있는 변경을 하면 아래 "현재 상태"를 갱신하세요.

사용자는 중학교 교사이고 개발 경험 없이 바이브코딩으로 진행 중입니다.
**설명보다 고치고 검증하고 배포까지 끝내는 것**이 이 프로젝트의 작업 패턴입니다.

## 뭐 하는 앱인가

교사가 주제를 넣으면 AI가 단어와 힌트를 만들어 가로세로 퀴즈 판을 짜고,
학생은 링크로 들어와 풀고 제출하면 자동 채점되어 교사 화면에 점수·소요시간·단어별 정답률이 쌓입니다.

- **교사용**: `https://crossword-theta.vercel.app/` (= `index.html`) — 비밀번호 잠금
- **학생용**: `https://crossword-theta.vercel.app/student.html?quiz=<퀴즈ID>`

## 구조 — 파일 두 개가 전부다

```
index.html            교사용 SPA. 퀴즈 만들기 / 저장된 퀴즈 / 제출 현황 / 사용 매뉴얼 4개 탭.
                      PDF·워드 출력(html2canvas·jsPDF), QR 코드(qrcodejs) 는 CDN 스크립트.
student.html          학생용. 그리드 입력 → 제출 → 즉시 채점 결과.
apps-script/Code.gs   백엔드 소스 사본. Vercel 은 이 파일을 쓰지 않는다 —
                      고쳐도 Apps Script 편집기에 붙여넣고 재배포해야 반영된다.
apps-script/README.md 배포 순서와 백엔드 설계 원칙. 서버를 건드리기 전에 읽을 것.
```

빌드 도구 없음. 프레임워크 없음. 순수 HTML 한 파일에 CSS·JS가 다 들어 있습니다.
**GitHub `main` 에 push 하면 Vercel 이 자동 배포**합니다. 별도 배포 명령 없음.

## 백엔드 = 구글 스프레드시트 + Apps Script

DB가 따로 없습니다. Google Apps Script 웹앱(`GAS_URL` 상수, 두 파일에 각각 하나씩 박혀 있음)에
JSON 을 POST 하면 스프레드시트를 읽고 씁니다. Gemini 호출도 GAS 안에서 합니다.

**Apps Script 코드는 `apps-script/Code.gs` 에 있습니다** (2026-09-10 부터).
단 이건 사본입니다 — 실제로 도는 건 구글 시트 → 확장 프로그램 → Apps Script 쪽이고,
사용자가 직접 붙여넣어야 반영됩니다. 서버를 고쳤으면 `Code.gs` 를 고쳐 커밋하고,
사용자에게 그 파일을 보내면서 `apps-script/README.md` 의 배포 순서를 안내하세요.

⚠️ **코드를 통째로 갈아끼우면 그 전에 넣어둔 것도 같이 지워집니다.**
실제로 2026-09-10 에 성능 패치를 붙여넣으면서 비밀번호 검사가 한 번 날아갔습니다.
`Code.gs` 를 최신으로 유지하고, 붙여넣은 뒤엔 README 의 잠금 테스트를 꼭 하세요.

`action` 목록:

| 학생이 쓰는 것 | 교사만 쓰는 것 |
|---|---|
| `getQuizForPlay`, `submitAnswer` | `generateWords`, `generateClues`, `saveQuiz`, `getQuizList`, `getQuizForEdit`, `deleteQuiz`, `getSubmissions`, `getWordStats` |

## 반드시 알아야 할 제약

1. **Apps Script 를 다시 배포할 때는 "새 배포"가 아니라 "배포 관리 → 편집 → 새 버전".**
   새 배포를 만들면 URL 이 바뀌어 이미 나눠준 학생 링크가 전부 죽습니다.
2. **구글 시트는 문자열을 멋대로 날짜로 바꾼다.** `"03:25"` 를 보내면 3분 25초가 아니라
   *3시 25분*으로 저장돼서 1899-12-30 기준 날짜가 됩니다. 그래서 소요시간은
   `student.html` 의 `fmtHuman()` 이 `"3분 25초"` 형태로 보냅니다. **`MM:SS` 로 되돌리지 마세요.**
3. **시트가 돌려주는 날짜는 UTC ISO 문자열**입니다. 그대로 자르면 9시간 어긋납니다.
   `index.html` 의 `kstParts()` / `fmtWhen()` 으로 한국 시각으로 바꿔 표시합니다.
4. **교사 페이지 잠금은 두 겹**입니다. 화면 잠금(`TEACHER_PW_HASH`, 클라이언트)만으로는
   막히지 않습니다 — 실제 방어는 Apps Script 가 `pw` 를 확인하는 것. `api()` 가 모든 요청에
   `pw` 를 실어 보냅니다. 이 필드를 빼지 마세요.
5. **한글 입력은 숨은 input 하나로 처리**합니다(`student.html` 의 `#hin`).
   칸마다 input 을 두면 안드로이드 크롬에서 조합이 깨집니다. 이 구조를 바꾸지 마세요.
6. 학생 페이지는 세로 열쇠 번호를 `ㄱㄴㄷ`, 가로는 `1 2 3` 으로 붙입니다(`KO_LABELS`).
7. **백엔드는 캐시로 버틴다.** 한 반 30명이 같은 링크를 열어도 시트는 한 번만 읽습니다
   (`CacheService`, 키는 `play_/row_/grade_/subcount`). **퀴즈 내용을 바꾸는 코드를 새로 쓰면
   반드시 `bustQuiz_(quizId)` 를 같이 부르세요.** 안 그러면 선생님이 퀴즈를 고쳐도
   학생은 최대 6시간 동안 옛 퀴즈를 받습니다. 자세한 건 `apps-script/README.md`.
8. **학생 화면은 서버를 기다리지 않는다.** `boot()` 가 학번/이름 화면을 먼저 그리고
   `fetchQuiz()` 가 뒤에서 받습니다. 이걸 되돌려 "받은 뒤에 그리기"로 만들면
   서버가 느릴 때 학생 눈에 빈 화면만 보입니다 — 원래 신고된 증상이 바로 이것이었습니다.
9. **제출에는 `attemptId` 가 붙습니다.** 통신이 끊겨 자동 재시도가 일어나도 서버가
   같은 `attemptId` 를 알아보고 기록을 두 번 남기지 않습니다. 이 필드를 빼지 마세요.
10. **모든 요청에 타임아웃과 재시도가 있습니다**(`apiOnce`/`api`). 타임아웃을 없애면
    Safari 가 무한 로딩에 빠졌다가 `Load failed` 만 던집니다.
11. **소요시간은 멈췄다 이어지는 시계입니다.** 제출하면 멈추고, [수정해서 다시 풀기] 를
    누르면 0 부터가 아니라 멈춘 지점부터 이어집니다(`state.baseSec` + `elapsedSec()`).
    `startTimer()` 에서 `state.startTime` 만 새로 잡으면 다시 풀 때마다 0 으로 리셋돼서
    **고칠수록 시간이 짧아지는** 불공평이 생깁니다 — 원래 그랬고, 2026-09-10 에 고쳤습니다.
    결과 화면 표시(`showResult`)와 서버로 보내는 값(`submitQuiz`) 모두 `elapsedSec()` 를 씁니다.
    ※ 그 전에 쌓인 기록에는 `0초`, `3초` 같은 재제출 값이 남아 있습니다(회귀 아님).

## 작업 방식

- 로컬 dev 서버 대신 정적 서버로 띄워 확인하면 됩니다: `npx http-server -p 8099`
- Playwright(크로미움 설치돼 있음)로 잠금 해제·시간 표시 같은 동작을 실제로 눌러 검증했습니다.
- 커밋 전 `git status` 로 비밀번호·키가 섞이지 않았는지 확인합니다.

## 현재 상태 (2026-09-10)

**최근 변경**
- 교사 페이지 비밀번호 잠금 추가. 비밀번호 `1111` (사용자 지정).
  잠금 화면의 "비밀번호 바꾸기"가 새 해시를 만들어 주고, 그 값을 `TEACHER_PW_HASH` 에 붙여넣습니다.
  헤더의 🔒 버튼으로 다시 잠급니다. 한 번 풀면 12시간 기억(localStorage `cwq_teacher`).
- 소요시간이 `1899-12-29T16:26:08.000Z` 처럼 보이던 문제 수정. 앞으로 들어오는 기록은
  `"1분 23초"` 로 저장되고, **이미 망가진 옛 기록은 `fmtTimeSpent()` 가 되돌려서 보여줍니다**
  (원래 분이 '시' 자리, 원래 초가 '분' 자리에 들어가 있음).
- 제출시각을 한국 시각으로 표시. CSV 내보내기도 같은 값으로 나갑니다.

**2026-09-10 — 동시접속 성능 (신고: 로그인 화면조차 안 뜸 / 제출 느림 / 제출현황 오류)**
원인은 Vercel 이 아니라 Apps Script ↔ 시트 구간이었습니다. 고친 것:
- 백엔드에 캐시 도입 — 두 번째 학생부터는 시트를 아예 건드리지 않습니다.
- 화면에서 쓰지 않는 열을 그만 읽습니다(퀴즈목록의 `quizData`, 제출목록의 `wordResults`).
- 제출 쓰기 재시도 + `attemptId` 중복 방지. 제출 목록은 최신 500건으로 자릅니다.
- 학생 화면이 서버를 기다리지 않고 먼저 뜹니다. 첫 요청은 ~1초에 걸쳐 흩뿌립니다.
- 모든 요청에 타임아웃·재시도. `"GAS 배포 권한..."` 이라는 잘못된 오류 문구도 고쳤습니다.

프로덕션 실측: 퀴즈 조회 1.35초(= `ping` 과 동일 = 시트 작업 0초),
30명 동시접속 30/30 성공·중앙값 2.9초·전원 7.9초 내 완료.
남은 1.3초는 Apps Script 자체 왕복이라 코드로는 못 줄입니다.

**`APPS_SCRIPT_보안패치.md` 는 적용 완료**입니다 — 이제 그 검사가 `Code.gs` 의 `handle()`
안에 `OPEN_ACTIONS` 로 들어가 있고, 비밀번호는 스크립트 속성 `TEACHER_PW` 에 있습니다.
그 문서는 이제 배경 설명용입니다.

**2026-09-10 (2) — 제출 현황 정렬 · 재제출 표시 · 소요시간 공평하게**
- 소요시간이 재제출 때 리셋되던 문제 수정(위 제약 11번).
- 제출 현황에 정렬 추가 — **퀴즈 하나를 골랐을 때만** 나옵니다(전체 보기는 퀴즈가 섞여
  줄세우기가 무의미). 이름순/학번순/제출시각순/소요시간순 + 오름·내림 토글.
  학번은 숫자로, 이름은 `localeCompare('ko')` 로, 소요시간은 `timeSpentSec()` 로 초를 만들어
  비교합니다. 값이 없는 행은 방향과 무관하게 항상 맨 아래로 보냅니다.
- 같은 학생이 다시 낸 기록은 이름 옆에 `✌🏻` + 몇 번째인지 표시(`markAttempts()`,
  학번으로 묶고 제출시각 순으로 번호를 매김). CSV 에도 `제출차수` 열이 나갑니다.
- `timeSpentSec()` 는 `fmtTimeSpent()` 와 **같은 세 가지 형태**를 해석해야 합니다.
  둘이 어긋나면 화면에 보이는 값과 정렬 순서가 따로 놀게 됩니다.

**다음에 할 일**
- **GitHub 저장소를 Private 으로** — 공개 상태라 Apps Script 주소가 소스에 노출돼 있습니다.
- 스프레드시트 → Firebase 이전은 **당분간 불필요**해 보입니다. 로딩이 느렸던 이유가
  데이터 양이 아니라 캐시 없음이었고, 그건 해결됐습니다. 다시 검토한다면 이유는
  속도가 아니라 Apps Script 의 콜드 스타트(첫 요청 5~15초)여야 합니다.
- 남은 가장 느린 요청은 `getSubmissions`(6~10초, 전체 시트 스캔). 기록이 더 쌓여
  거슬리면 그때 짧은 캐시(15~20초)를 붙이면 됩니다.
